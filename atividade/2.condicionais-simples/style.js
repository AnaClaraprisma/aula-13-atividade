function mostrarResultado(resultado) {
    document.getElementById("input").addEventListener(resultado);
}

let resultado = ""

function parOuImpar(a) {
    if (a % 2 == 0) {
        return `${a} é par`

    } else {
        `${a} é impar`
    }
    mostrarResultado(resultado(4))
    document.getElementById("but")
}